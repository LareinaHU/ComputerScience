package Rubbish;

public class hhh {
    public static void main(String[] args) {
        int x = 0, y = 1;
        for (int i = 0; i < 2; i++) {
            while (x <= 1) {
                y++;
                x++;
                System.out.println(x + "**" + y);
            }
            System.out.println(x + "+" + y);
        }
        System.out.println(x + ";" + y);
    }
}
